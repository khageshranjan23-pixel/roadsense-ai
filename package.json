{
  "name": "roadsense-ai",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/shared-types"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@roadsense-ai/web",
    "build": "npm run build --workspace=@roadsense-ai/web",
    "test": "npm run test --workspace=@roadsense-ai/web"
  }
}
